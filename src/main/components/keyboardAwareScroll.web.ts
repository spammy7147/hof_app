/**
 * 웹의 입력 포커스와 키보드 스크롤은 브라우저가 처리한다.
 * 네이티브 키보드 보정을 호출하면 버튼이나 포털의 focus도 배경 목록을 맨 위로 이동시킨다.
 */
export function scrollFocusedInputIntoView(
  _scrollable: unknown,
  _target: unknown,
  _additionalOffset = 24,
): void {}
